# OnlyStaff — деплой на Netlify

## Структура
```
index.html                     — лендинг (pl/ru переключение языка встроено)
google-apps-script/Code.gs     — приём заявок в Google Таблицу
assets/favicon.svg, apple-touch-icon.png, og-image.jpg — сделаны как рабочая
                                  заглушка в цветах сайта (тёмно-синий #0E151D + акцент #2445D6),
                                  замените на брендовые, когда будут
```

## Что уже сделал (обновление — домен и юрданные подключены)

- Домен `onlystaff.pl` — canonical, hreflang, og:url, `CONFIG.siteUrl`
- `CONFIG.company`, `email`, `privacyUrl`, `impressumUrl`, `cookiesUrl` — заполнены
- Сделал `privacy.html` и `impressum.html` — на данных MBUD sp. z o.o. (то же юрлицо, что в MBUD START),
  стандартные формулировки по DSGVO/RODO, как в прошлом проекте
- `og:image` подключен на сгенерированный файл-заглушку

## Осталось реально нужное от вас

1. **Google Apps Script** — задеплоить `Code.gs` и вставить URL в `CONFIG.formEndpoint` (шаги ниже, без этого форма не работает)
2. **Телефон/WhatsApp для самого лендинга** (`CONFIG.phone`, `CONFIG.whatsapp`) — сейчас пустые, кнопки звонка/WhatsApp скрыты.
   В Impressum уже стоит общий телефон MBUD (+48 578 573 836) как юридический контакт,
   но для кнопок на самом лендинге может быть удобнее отдельный номер под этот продукт — решать вам
3. **Impressum** — ФИО ответственного за контент, физлицо от компании (обычно член правления)
4. **Факт-чек** — секция «О нас», подтвердить, что текст про опыт компании реальный
5. Медиа/фото объекта — в коде есть готовое место под фото (`.about-fig`, закомментировано), не обязательно

## 1. Google Apps Script — 5 минут

1. Создайте новую Google Таблицу.
2. Расширения → Apps Script → вставьте `google-apps-script/Code.gs`.
3. (Опционально) впишите e-mail в `NOTIFY_EMAIL` в начале файла — будет письмо на каждую заявку.
4. Развернуть → Новое развёртывание → тип **Веб-приложение**, доступ **Все (Anyone)**.
5. Скопируйте URL (`/exec`) → вставьте в `index.html` → `CONFIG.formEndpoint`.

## Что вписать в index.html — то, что реально нужно от вас, см. список выше

Google Fonts (Inter + Unbounded для логотипа) подключены напрямую с
fonts.googleapis.com, и в этой версии лендинга нет баннера согласия на cookie —
то есть шрифты (а значит IP посетителя) уходят к Google ещё до какого-либо
согласия. В MBUD START я это чинил (самостоятельный хостинг шрифтов + баннер).
Здесь не стал трогать сам — логотип завязан на шрифт Unbounded, и без баннера
согласия переделка шрифтов не решает проблему полностью. Скажите, если нужно
разобрать это подробно — либо добавить баннер, либо перейти на системные шрифты.
